global _start
#include <stdio.km>
#include <string.km>
#include <math.km>

fn _start() : byte {
	let name : char[512]
	print("What is your name: ")
	readln(name, 512)
	if streq(name, "test") print("[TEST] ")
	for i = 0 -> 9 ^ 1 {
		printi(i + 1)
		print(": Hello, ")
		print(name)
		print("!\n")
	}
	return 0
}